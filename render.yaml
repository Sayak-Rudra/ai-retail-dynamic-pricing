services:
  - type: web
    name: dynamic-pricing-app
    runtime: python
    plan: free

    buildCommand: pip install -r requirements.txt

    startCommand: gunicorn app:app

    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9

      - key: FLASK_APP
        value: app.py